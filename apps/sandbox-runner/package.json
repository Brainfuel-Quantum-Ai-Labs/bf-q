{
  "name": "bf-q-sandbox-runner",
  "version": "0.1.0",
  "private": true,
  "description": "Sandbox runner service — executes isolated build/run jobs via HTTP",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "express-rate-limit": "^7.5.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
